machine:
  ruby:
    version: 2.3.0
dependencies:
  bundler:
    without: [development]
database:
  override:
    - mkdir tmp
    - mkdir tmp/sockets
test:
  override:
    - SIM_ENV=test bundle exec rake spec
